- name: JMeter Test
  uses: SruthySJ/GithubActions
  with:
    test-plan-path: ./Desktop/Thread Group.jmx
    args: ""
- name: Upload Results
  uses: actions/upload-artifact@v3
  with:
    name: jmeter-results
    path: result.jtl
    if-no-files-found: error
